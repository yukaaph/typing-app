# 設定確認表 GAS版プロトタイプ

`settings-checklist/`（localStorageのみで動く静的プロトタイプ）と同じ画面・機能を、Google Apps Script（GAS）+ Google Sheetsで動かす版です。認証（ログイン）とデータ保存をSheetsで行います。

## 認証方式：パスワードのみ（ID不要）＋自治体ごとのデータ分離

SharePointの「パスワード付きリンク共有」と同じ考え方で、**ユーザーIDはなく、パスワードだけでログイン**します。パスワードごとに役割（お客様／社内担当者）と表示名が紐づいており、パスワード自体が「誰か」を判定する鍵になります。

- パスワードは**英数字15桁のランダム文字列**。スプレッドシートのメニュー「設定確認表 管理」から発行できます（`Admin.gs`）。
- 発行時にその場で一度だけ表示されます（Usersシートにはハッシュ化されたものしか残らないため、後から見返すことはできません）。控えてリンクと一緒にお客様へお伝えください。
- 紛失した場合は同じ画面からもう一度発行し直してください（同じ人に対して複数のパスワードが有効な状態でも問題ありません）。
- **お客様用パスワードは、発行時に自治体専用の新しい契約データ（ContractStateシートの1行）を自動作成し、そのパスワードに紐づけます。** 他の自治体のパスワードでログインしても、この契約データは一切見えません（サーバー側で強制しており、クライアント側の改ざんでは回避できません）。
- **社内担当者用パスワードはどの自治体にも紐づかず、ログイン後に表示される「契約一覧」からすべての自治体の契約を選んで確認・承認できます。**

## デプロイ済み環境（clasp経由で作成）

`irukattiw1@gmail.com` アカウントに、claspで以下を作成・push・デプロイ済みです。

- Spreadsheet（DB）: https://docs.google.com/spreadsheets/d/1ToHBYOqGuPaUPE5cNSFaJp8-hTqeyrs-eCj7W_U2T78/edit
- Apps Scriptエディタ: https://script.google.com/d/1LtF4iu-vZ6nSjAb1oBObFyjChY7pNYgMkDiD4mmDEn2L14qQs1nMkDn5/edit
- Webアプリ（公開URL）: https://script.google.com/macros/s/AKfycbz_ZGNWFp709Irh1i3H17KtZ8RnBd1mLijpClm9QNwDbhzGBA7WU0MqxN-Bo00BQMBG/exec

**Usersシート／ContractStateシートの構造が変わりました（自治体ごとのデータ分離に対応）。** 反映するには、上記のApps Scriptエディタで `setupSpreadsheet` をもう一度▶実行してください（`clasp run` はGoogleアカウント個人設定でのApps Script API有効化が必要でCLIから自動化できないため、この一手だけ手動でお願いします）。実行後、シートを開くと上部メニューに **「設定確認表 管理」** が追加され、そこから自治体ごとのパスワード・契約を発行できます（メニューが出ない場合はシートを開き直してください）。

実行後、Webアプリの公開URLを開けば、パスワードのみの入力画面になります。デモパスワード（`demoCustomer123` / `demoStaff123456`）でのワンクリックログインボタンも引き続き使えます。

以降、コードを修正した場合は `settings-checklist-gas/` で `clasp push` すればこのプロジェクトに反映されます（`.clasp.json` が既にこのプロジェクトを指しています）。デプロイ済みURLに反映するには `clasp deploy` を再実行してください。

## 構成

| ファイル | 役割 |
|---|---|
| `appsscript.json` | プロジェクトのマニフェスト（Webアプリの公開設定など） |
| `Code.gs` | `doGet` エントリポイント、HTML部品の読み込み |
| `Auth.gs` | パスワードのみのログイン／ログアウト、パスワード発行、セッション管理（CacheService） |
| `Admin.gs` | スプレッドシートのカスタムメニュー（パスワード発行UI） |
| `DataService.gs` | Sheets（ContractStateシート）への状態の読み書き |
| `Api.gs` | クライアントから呼ばれるAPI関数（`google.script.run` 経由） |
| `Setup.gs` | 初回セットアップ用（Users/ContractStateシートの初期化、デモパスワードの登録） |
| `Index.html` | 画面全体のHTML（ログイン画面＋メイン画面） |
| `Stylesheet.html` | CSS（`settings-checklist/style.css` と同内容） |
| `JavaScriptClient.html` | クライアント側JS（元プロトタイプの画面ロジック＋サーバー呼び出し） |

## データの持ち方

- **Users シート**：`passwordHash / role / displayName / contractId / createdAt` の5列。ログイン時にパスワードのSHA-256ハッシュをこの表と照合し、一致した行の `role`・`displayName`・`contractId` をセッションに載せます。IDは持たず、パスワードのハッシュそのものが検索キーです。お客様行の `contractId` はその自治体専用の契約を指し、社内担当者行は空欄（＝全契約アクセス）です。
- **ContractState シート**：`contractId / stateJson / updatedAt` の3列。**自治体（契約）ごとに1行**、設定内容（元プロトタイプの `state` とほぼ同じ形。`contract.customerName` に自治体名を保持）をJSON文字列としてまるごと1セルに保存します。行の作成はお客様用パスワードの発行時にのみ行われ、存在しない契約IDへのアクセスはエラーになります（＝勝手に空の契約が生えることはありません）。

## 新しいパスワード（＝自治体）の発行方法

1. 上記のSpreadsheetを開く
2. メニュー「設定確認表 管理」→「お客様用パスワードを発行（自治体を新規登録）」（または「社内担当者用パスワードを発行」）
3. 表示名（自治体名・担当者名など）を入力
4. お客様用の場合、その自治体専用の新しい契約データが自動作成され、パスワードと契約IDが表示されます。控えて、リンクと一緒にお客様へ共有する
5. 社内担当者用の場合、発行したパスワードでログインすると「契約一覧」からすべての自治体を確認できます

Apps Scriptエディタから直接発行したい場合は、`Auth.gs` の `issueAccessPassword_(role, displayName, contractId)` と `DataService.gs` の `createContractState_(contractId, customerName)` / `generateContractId_()` を組み合わせて呼び出しても同じことができます。

## デプロイ手順（ゼロから構築する場合）

1. **Google Sheetsを新規作成**します（例：「設定確認表_DB」）。
2. そのシートのメニューから **拡張機能 > Apps Script** を開きます（このシートに紐づいたスクリプトプロジェクトが作られます）。
3. エディタで、このフォルダ内の各ファイルと同じ名前・内容のファイルを作成します。
   - `appsscript.json` は、エディタ左の歯車アイコン「プロジェクトの設定」で「`appsscript.json`マニフェストファイルをエディタで表示する」をONにすると編集できるようになります。
   - `.gs` ファイル（`Code.gs` `Auth.gs` `Admin.gs` `DataService.gs` `Api.gs` `Setup.gs`）は「ファイル > 新規 > スクリプト」で作成し、中身をコピーします。
   - `.html` ファイル（`Index.html` `Stylesheet.html` `JavaScriptClient.html`）は「ファイル > 新規 > HTML」で作成し、中身をコピーします（拡張子`.html`は自動で付きます）。
   - `clasp` (`npm install -g @google/clasp`) をお使いの場合は、このフォルダをそのまま `clasp push` していただいても構いません。
4. 関数選択のプルダウンで **`setupSpreadsheet`** を選び、▶実行します。初回は権限の承認を求められるので許可してください。
   - 実行後、シートに `Users` と `ContractState` の2つのタブが作成され、上部メニューに「設定確認表 管理」が追加されます。
   - `Users` シートには、デモパスワード（お客様用: `demoCustomer123`・デモ自治体専用、社内担当者用: `demoStaff123456`・全契約アクセス）が登録されます。
5. **デプロイ > 新しいデプロイ** を開き、種類で「ウェブアプリ」を選択します。
   - **実行するユーザー**：自分（オーナー）
   - **アクセスできるユーザー**：全員
   - この組み合わせが必要です。「全員」にするのは、お客様が社外の方でGoogleアカウントを持たない前提のためです（ログインはGoogleアカウントではなく、独自のパスワードのみで行います）。
6. デプロイ後に発行される **ウェブアプリのURL**（`.../exec` で終わるURL）を開くと、パスワード入力画面が表示されます。

## デモパスワード

| パスワード | ロール | 対象 |
|---|---|---|
| `demoCustomer123` | お客様 | デモ自治体専用（1契約のみ） |
| `demoStaff123456` | 社内担当者 | 全契約アクセス（契約一覧からすべて確認可） |

（実運用のパスワードは「設定確認表 管理」メニューからランダムに発行してください。上記のデモ用は固定文字列にしているので、ログイン画面の「お客様用デモでログイン」「社内担当者用デモでログイン」ボタンから毎回同じ値で試せます。）

## 画面の流れ

- **お客様**：ログインすると自分の自治体の契約画面に直接入ります（他の自治体の契約は選ぶことも見ることもできません）。
- **社内担当者**：ログインすると「契約一覧」（自治体名・契約ID・状態の一覧）が表示されます。行の「開く」から契約を選ぶと、その自治体のデータの確認・承認ができます。サイドバー上部の「契約一覧」からいつでも一覧に戻れます。

## 元プロトタイプとの主な違い

- 認証にIDがなく、パスワードのみでログインします。
- **自治体（契約）ごとにデータが完全に分離されています。** 静的プロトタイプ／GAS版の初期実装は単一の共有デモデータでしたが、今はお客様用パスワードの発行ごとに専用の契約データが作られ、サーバー側でアクセス範囲が強制されます。
- 社内担当者はログイン後に契約一覧から対象の自治体を選ぶ、多契約対応のフローになっています。
- ログイン中のロール（お客様／社内担当者）でできることが**サーバー側でも**強制されます（元の静的プロトタイプはブラウザ側のチェックのみでした）。
- 「デモデータをリセット」は、選択中の契約のデータだけを初期化します。ログイン状態はそのまま維持されます。
- データはブラウザのlocalStorageではなく、Google Sheets上に保存されます。複数人が同時に開いても（同じ契約であれば）同じデータを見ます。

## 制限・注意点

- **本番グレードの認証ではありません。** パスワードのハッシュ化はしていますが、ログイン試行制限・多要素認証・監査ログなどはありません（ただしパスワード自体が高エントロピーなランダム15桁なので、人が選ぶパスワードよりは推測されにくい設計です）。実際の顧客データを扱う本番環境では、Google Identity Platform や外部の認証基盤（Auth0, Firebase Authなど）の採用を推奨します。
- 発行したパスワードの平文はどこにも保存されません（発行時に一度だけ画面表示されます）。紛失時は再発行してください。
- 1つのパスワードで複数契約にまたがることはできません（お客様は常に1契約、社内担当者は契約一覧から都度選択）。複数自治体を1つのパスワードでまとめて見せたい、といった要望があれば別途対応します。
- セッションは `CacheService` を使っており、最大6時間で自動的に切れます（Apps Scriptの仕様上の上限です）。
- このリポジトリの環境ではApps Scriptの実行・デプロイができないため（`clasp`を使い一部は自動化しましたが）、**すべての機能を実機で確認できているわけではありません。** エラーが出た場合は、エラーメッセージ（Apps Scriptエディタの「実行数」ログ、またはブラウザのコンソール）を共有してください。
